Questa cartella e pronta per ospitare preset futuri serializzati su file.

La v1 usa preset definiti in codice nel modulo `core/presets.py`, ma l'architettura e gia predisposta per spostarli qui in JSON o YAML.
